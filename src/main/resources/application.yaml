server:
 port: 8003
spring:
 datasource:
  url: jdbc:mysql://localhost:3306/db_springboot?serverTimezone=America/Mexico_City
  username: root
  password: 0000
  driver-class-name: com.mysql.cj.jdbc.Driver jpa:  database-platform: org.hibernate.dialect.MySQL8Dialect  hibernate:   ddl-auto: create-drop
logging:
 level:
  org:
   hibernate:
    SQL: debug